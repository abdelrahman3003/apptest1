class RegisterModel {
   String? title, id, email,phone, image, date, time, visitReason;

  RegisterModel(
      {this.title,
      this.id,
      this.email,
      this.image,
      this.date,
      this.time,
      this.visitReason});
}
